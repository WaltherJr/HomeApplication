<!DOCTYPE html>
<html lang="{{ documentLanguage }}">
<head>
    <meta charset="{{ documentCharacterSet }}">
    <title>{{ documentTitle }}</title>
</head>
<body>
    <div id="devicesList">{{ roomDevices | raw }}</div>
</body>
</html>
